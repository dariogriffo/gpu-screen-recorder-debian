Source: gpu-screen-recorder
Section: video
Priority: optional
Maintainer: Dario Griffo <dariogriffo@gmail.com>
Build-Depends:
 debhelper-compat (= 13),
 meson,
 pkgconf,
 libwayland-bin,
 libavcodec-dev,
 libavformat-dev,
 libavutil-dev,
 libavfilter-dev,
 libswresample-dev,
 libx11-dev,
 libxcomposite-dev,
 libxrandr-dev,
 libxfixes-dev,
 libxdamage-dev,
 libpulse-dev,
 libva-dev,
 libdrm-dev,
 libwayland-dev,
 libcap-dev,
 libpipewire-0.3-dev,
 libspa-0.2-dev,
 libdbus-1-dev,
 libvulkan-dev,
Standards-Version: 4.7.0
Homepage: https://git.dec05eba.com/gpu-screen-recorder/about/
Rules-Requires-Root: no

Package: gpu-screen-recorder-cli
Architecture: amd64
Depends:
 ${misc:Depends},
 ${shlibs:Depends},
 libcap2-bin,
# Named after Debian's (forky/sid) binary package, which ships the program
# split into several packages that install the same paths as this one.
Conflicts:
 gpu-screen-recorder-dev,
 gpu-screen-recorder-scripts,
 gpu-screen-recorder-service,
Replaces:
 gpu-screen-recorder (<< 6.1.3-2~),
 gpu-screen-recorder-dev,
 gpu-screen-recorder-scripts,
 gpu-screen-recorder-service,
Breaks:
 gpu-screen-recorder (<< 6.1.3-2~),
Description: Shadowplay-like screen recorder that records using the GPU only
 GPU Screen Recorder records a monitor or window with minimal impact on system
 performance by capturing and encoding on the GPU, similar to ShadowPlay on
 Windows. It works on X11 and Wayland with AMD, Intel and NVIDIA GPUs, and
 supports replay buffers, streaming and screenshots.
 .
 This package includes the gpu-screen-recorder and gsr-cli commands, the
 gsr-kms-server helper, the plugin header, upstream's helper scripts and a
 systemd user service.

# Earlier releases shipped everything as gpu-screen-recorder; this keeps
# upgrades moving to the renamed package.
Package: gpu-screen-recorder
Section: oldlibs
Architecture: all
Depends:
 ${misc:Depends},
 gpu-screen-recorder-cli (>= ${source:Version}),
Description: Shadowplay-like screen recorder (transitional package)
 This is a transitional package that installs gpu-screen-recorder-cli. It can
 safely be removed.
